POS Parfum Refill - Implementation Workspace

Status: scaffolded backend and DB schema. All todos marked in_progress.

Next steps:
- Install backend dependencies: cd backend && npm install
- Run: npm run start then POST /api/init-db to create DB
- Implement API endpoints for products, variants, transactions, recipes, waste, and vendor flows
- Implement frontend (React) and wire to backend

Notes:
- DB path: backend/data/pos.db (default)
- Use UUIDs for IDs
